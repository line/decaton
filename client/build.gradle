dependencies {
    compile project(":protocol")
    compile project(":common")

    compile "org.apache.kafka:kafka-clients:$kafkaVersion"
    compile "io.zipkin.brave:brave-instrumentation-kafka-clients:$braveVersion"

    testCompile project(":protobuf")
}
